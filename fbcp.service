[Unit]
Description=Framebuffer Copy Service (HDMI to SPI LCD)
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/fbcp
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target
